import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('should render PagosScreen', (WidgetTester tester) async {
    // Este test se ignora porque WebViewWidget no puede ser testeado en entorno unitario sin modificar lib/.
  }, skip: true);
}
