import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Empty test to satisfy test runner', () {
    // Esta prueba no hace nada, solo existe para satisfacer al runner de pruebas
    expect(true, isTrue);
  });
}