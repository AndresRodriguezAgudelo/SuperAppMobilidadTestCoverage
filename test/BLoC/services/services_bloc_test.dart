import 'package:flutter_test/flutter_test.dart';

void main() {
  group('ServicesBloc Tests', () {
    test('should initialize ServicesBloc and have default state', () {
      // Suponiendo que tienes ServicesBloc implementado en lib/BLoC/services/services_bloc.dart
      // y tiene un estado inicial conocido
      // Reemplaza esto con la lógica real de tu ServicesBloc
      // Por ejemplo:
      // final bloc = ServicesBloc();
      // expect(bloc.services, isEmpty);
      // expect(bloc.isLoading, isFalse);
      // expect(bloc.error, isNull);
      expect(true, isTrue); // Cambia esto por asserts reales cuando tengas el bloc
    });
  });
}
