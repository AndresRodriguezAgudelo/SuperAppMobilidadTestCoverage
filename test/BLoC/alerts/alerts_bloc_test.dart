 import 'package:flutter_test/flutter_test.dart';
 void main() {
   group('AlertsBloc Tests', () {
     test('should initialize AlertsBloc and have default state', () {
       // Suponiendo que tienes AlertsBloc implementado en lib/BLoC/alerts/alerts_bloc.dart
       // y tiene un estado inicial conocido
       // Reemplaza esto con la lógica real de tu AlertsBloc
       // Por ejemplo:
       // final bloc = AlertsBloc();
       // expect(bloc.alerts, isEmpty);
       // expect(bloc.isLoading, isFalse);
       // expect(bloc.error, isNull);
       expect(true, isTrue); // Cambia esto por asserts reales cuando tengas el bloc
     });
   });
 }